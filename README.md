# Automate-Server-Configuration-Ansible-Roles-Jinja2-Idempotent-Nginx-Node-Deployment
Automated server configuration and application deployment using Ansible role-based architecture — common hardening, Nginx with Jinja2 templates, EpicBook Node.js app deployment via systemd — fully idempotent, with Terraform provisioning the underlying Azure VMs.
